# build output
dist/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# playwright
test-results/
playwright-report/
playwright/.cache/

# pair-review screenshots (local-only, visual diffing vs live Webflow)
screenshots/

# one-off diagnostic scripts (local-only, written per drift-fix batch
# to measure DOM differences between local and live — see CLAUDE.md).
# Only the top-level /scripts dir, not src/scripts (which holds client JS).
/scripts/

# local preview launch config
.claude/

*storybook.log
storybook-static
